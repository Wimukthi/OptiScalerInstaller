## OptiPatcher Support List (Fallback)

This bundled file is a fallback used when no cached list exists and the online list cannot be reached.
Use `Refresh lists` in the app to pull the latest support data.

| Game | DLSS | DLSS-FG | Notes |
| ---- | :--: | :-----: | ----- |
